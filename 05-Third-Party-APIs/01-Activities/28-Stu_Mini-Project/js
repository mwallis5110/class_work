Pseudocode 
1. User inputs info into a form
    event.preventDefault
2. User clicks buttom
When button is clicked (event listener on button):
1. Grab form values, store as variable
    var email =
    var password =
2. Use variable to setItem in local storage
3. Use getItem to read from local storage
4. Might need to parse before appending to page
5. Set HTML element as var

6. Set html element give class, data-attr, textContent, innerHTML


